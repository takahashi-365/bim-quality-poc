# BIM Data Quality Engineering & AI Analysis PoC

## 管理情報

PoC管理フォルダ：  
`bim_quality_poc`

PoC名：  
`BIM Data Quality Engineering & AI Analysis PoC`

## 概要

このPoCは、Revit/BIMデータをPythonで処理し、BIM品質ルールに基づく品質チェック、品質メトリクス作成、特徴量化、簡易機械学習、生成AI向け構造化コンテキスト生成までを検証するものです。

目的は、建築BIMデータをAI・機械学習・データ分析で扱うために必要となる、データ整形、ルールベース判定、品質評価、特徴量作成、品質スコア算出、修正優先度分類、生成AI連携前処理の一連の流れを実装することです。

現時点では、品質チェック用に整形されたBIMデータCSVを入力として使用し、Revitから書き出した集計表TXTをPythonで読み込み、品質チェック用CSVへ変換したうえで、品質チェック結果CSVを出力する初期試作まで確認済みです。

本PoCでは、BIMデータを単なる設計情報ではなく、AI・機械学習・データ分析で扱うための構造化データとして捉え、Pythonによるデータ処理、品質評価、特徴量設計、AI活用前提のデータ設計を行います。

なお、本PoCはAIモデルそのものの精度を追求するものではなく、建築BIMデータをAI・機械学習で扱うために必要なデータ整形、品質評価、特徴量設計、分析、簡易分類モデル、生成AI連携前処理までの一連の流れを検証するものです。

## 目的

このPoCの目的は、BIM/Revit導入支援の経験を活かしながら、建築BIMデータをAI・機械学習・データ分析で扱うためのデータ処理パイプラインを設計・実装することです。

単なるBIM品質チェックツールではなく、Revit/BIMデータをPythonで読み込み、データ整形、品質チェック、品質メトリクス作成、特徴量化、品質スコア算出、簡易機械学習、生成AI向け構造化コンテキスト生成へつなげる一連の流れを検証します。

特に、以下の点を重視しています。

- PythonによるRevit/BIMデータ処理
- pandasによるデータ読み込み・整形・欠損処理
- RuleIdを用いたBIM品質ルール管理
- ルールベースによるBIMデータ品質チェック
- BIM品質チェック結果のCSV出力
- 品質メトリクス、品質スコア、特徴量データセットの作成
- scikit-learnを用いた簡易機械学習モデルへの拡張
- Power BIまたはStreamlitによる分析結果の可視化
- RuleId、違反内容、重大度、修正優先度をもとにした生成AI向け構造化コンテキスト生成
- 将来的なRAG、Azure AI、ローカルLLM、pyRevit、Revit API連携への拡張

## このPoCで示したいこと

- Revit/BIMデータをPythonで読み込み、構造化データとして処理できる
- Revit集計表TXTをpandasで読み込み、品質チェック用CSVへ変換できる
- BIM品質ルールをRuleIdとして定義し、ルールベース判定を実装できる
- BIM導入支援で扱ってきた品質ルールや運用ルールを、Pythonで処理可能なデータとして整理できる
- pandasを用いてデータの読み込み、欠損判定、分類、集計ができる
- BIM品質チェック結果をRuleId、重大度、修正ガイド付きのCSVとして出力できる
- BIM品質チェック結果から、品質メトリクスや分析用データを作成できる
- 要素ごとの違反数、未入力項目数、重大度スコア、品質スコアなどの特徴量を設計できる
- BIM品質チェック結果を、機械学習や分析に利用できる特徴量データセットへ変換できる
- 修正優先度 High / Medium / Low の仮ラベルを設計し、簡易分類モデルへ拡張できる
- scikit-learnを用いた修正優先度分類モデルの試作、評価、限界整理へ発展できる
- Power BIまたはStreamlitで、品質指標、違反傾向、修正優先度を可視化できる
- RuleId、違反内容、重大度、修正優先度、修正方針をもとに、生成AI向け構造化コンテキストを生成できる
- 生成AIに自由回答させるのではなく、参照ルールや入力情報を制御したAI活用設計ができる
- 将来的なRAG、Azure AI、ローカルLLM、pyRevit、Revit API連携に拡張できる設計にする

## 現時点の入力データについて

現時点の `check_bim_quality.py` は、Revit書き出しTXTを直接読み込むのではなく、品質チェック用に整形されたCSVを入力データとして使用しています。

当初は、Python品質チェックロジックの検証用として、手作成のサンプルCSV `sample_bim_data_v001.csv` を入力に使用しました。

その後、Revitから書き出した集計表TXT `door_schedule_SD_export_test_v001.txt` を `convert_revit_schedule.py` で品質チェック用CSV `door_schedule_converted_v001.csv` へ変換し、そのCSVを `check_bim_quality.py` に入力して、品質チェック結果 `check_results_revit_v001.csv` を出力できることを確認しました。

現時点の処理フローは以下です。

```text
Revit書き出しTXT
↓
convert_revit_schedule.py で品質チェック用CSVへ変換
↓
door_schedule_converted_v001.csv
↓
check_bim_quality.py で品質チェック
↓
check_results_revit_v001.csv を出力
```

この構成により、Revit/BIM由来のデータをPythonで前処理し、品質チェック、分析、AI活用につなげる流れを段階的に構築しています。

なお、現時点のRevit由来データ対応は初期試作であり、列マッピングは仮設定です。今後、Revit集計表の各列の意味を確認し、正式な列マッピングを整理します。

## 使用データ

Autodesk日本仕様 意匠サンプルモデル Revit 2024を検証用データとして使用します。

実案件データ・社外秘データは使用しません。

## 対象カテゴリ

初期PoCでは、対象カテゴリを以下の3つに限定します。

- 壁 / Walls
- ドア / Doors
- 部屋 / Rooms

## 既存集計表確認時の補足

初期PoCの設計上の対象カテゴリは「壁・ドア・部屋」とします。

ただし、5月時点で確認した既存集計表では、壁要素を直接一覧化した集計表が見当たらなかったため、CSV書き出しテストでは代替確認として「04 材料表 作業用」を使用しています。

これは5月時点の書き出し確認用の扱いであり、必要に応じて後続工程で壁集計表を新規作成します。

## 対象外カテゴリ

初期PoCでは、以下は対象外とします。

- 床
- 天井
- 窓
- 家具
- 設備
- 構造
- 外構

## 初期RuleId

- R-001：必須パラメータ未入力
- R-002：分類コード未入力
- R-003：ファミリ命名規則違反

## RuleIdルールマスタ

### ファイル名

`02_rule_master/bim_rule_master_v001.csv`

### 目的

Python品質チェック、Power BI可視化、RuleId検索デモ、生成AI向けプロンプト生成デモで共通して使用するチェックルールIDを定義します。

### 初期ルール

- R-001：必須パラメータ未入力
- R-002：分類コード未入力
- R-003：ファミリ命名規則違反

### 補足

初期版では3ルールのみを定義します。

ルール数を増やすことよりも、RuleIdを共通キーとして、Pythonチェック結果、Power BI可視化、生成AI向けプロンプト生成を接続することを重視します。

## Revit集計表書き出し確認

### 検証用モデル

`BIM_Quality_Check_Sample_Model_R2024_v001.rvt`

### 目的

Autodesk日本仕様 意匠サンプルモデルから、PoC用の集計表データを書き出せるか確認します。

### 確認済み集計表

- 20 ドア 建具表 SD  
  出力ファイル：`03_input_csv/door_schedule_SD_export_test_v001.txt`

- 04 材料表 作業用  
  出力ファイル：`03_input_csv/material_schedule_export_test_v001.txt`

- 05 部屋 仕上表  
  出力ファイル：`03_input_csv/room_finish_schedule_export_test_v001.txt`

### 確認結果

上記3ファイルは、Revitからタブ区切りテキストとして書き出し、Excelで開けることを確認済みです。

### 補足

5月時点では、Revitから集計表を書き出せるかを確認する目的で、既存集計表を使用しています。

Python品質チェック用の入力CSVとして使用する場合は、後続工程で列名や不要な見出し行を整理します。

## 入力CSV初版

### ファイル名

`03_input_csv/sample_bim_data_v001.csv`

### 目的

Python品質チェックの初期実装に使用する検証用サンプルCSVです。

### 内容

Doors、Rooms、Walls の3カテゴリを含みます。

R-001、R-002、R-003 のチェックを検証できるよう、意図的に未入力や命名規則違反を含めています。

### 主な列

- ElementId
- Category
- FamilyName
- TypeName
- Level
- BIM_ClassificationCode
- BIM_ModelRole
- BIM_Zone
- SourceFile
- ModelName

### 意図的に含めた検証用エラー

- BIM_ClassificationCode 未入力
- BIM_ModelRole 未入力
- BIM_Zone 未入力
- FamilyName の命名規則違反

### 補足

このファイルはPython品質チェックロジックを作成するための検証用CSVであり、Revitから直接書き出したデータではありません。

Revit由来データは、後続工程で既存集計表出力データを整形して使用する予定です。

## Python実行環境

### 使用バージョン

Python 3.12.10

### 使用ライブラリ

pandas 3.0.3

### 用途

BIM品質チェックツールを実行し、入力CSVを読み込み、RuleId付きのチェック結果CSVを出力します。

### 補足

現時点ではPC全体のPython環境にpandasをインストールしています。  
今後、必要に応じて仮想環境 `.venv` を使用します。

## requirements.txt

### ファイル名

`requirements.txt`

### 配置場所

`bim_quality_poc` の直下に配置します。

### 記載内容

```text
pandas==3.0.3
```

### 目的

Python品質チェックツールの実行に必要な外部ライブラリを明示し、別環境でも再現しやすくします。

### 補足

現時点では、CSV読み込み、データチェック、チェック結果CSV出力に pandas を使用します。

Excelファイルの直接読み書きやExcelレポート出力を追加する場合は、将来的に openpyxl の追加を検討します。

## セットアップ手順

PowerShellで、`bim_quality_poc` フォルダへ移動します。

```powershell
cd path\to\bim_quality_poc
```

`requirements.txt` を使用して、必要なライブラリをインストールします。

```powershell
pip install -r requirements.txt
```

pandas のバージョンを確認します。

```powershell
python -c "import pandas as pd; print(pd.__version__)"
```

確認結果：

```text
3.0.3
```

## Python品質チェックツール 初期版

### 作成ファイル

`08_python/check_bim_quality.py`

### 入力CSV

`03_input_csv/sample_bim_data_v001.csv`

### ルールマスタ

`02_rule_master/bim_rule_master_v001.csv`

### 出力CSV

`04_output_csv/check_results_v001.csv`

### 実行コマンド

```powershell
python .\08_python\check_bim_quality.py
```

### 実行結果

```text
9 issues found
```

### 検出対象ルール

- R-001：必須パラメータ未入力
- R-002：分類コード未入力
- R-003：ファミリ命名規則違反

### 確認結果

PowerShellで実行でき、`check_results_v001.csv` が出力されました。

出力CSVには、RuleId、RuleName、Severity、Status、FixGuide、DetectedAt が含まれています。  
Excelで開けることも確認済みです。

### 補足

初期版ではRevit APIや自動修正は行わず、CSVを読み込み、BIM品質ルールに基づいて品質チェック結果を出力することを目的とします。

この処理は、BIMデータをAI・機械学習・データ分析に利用するための前処理として位置づけます。

## Revit由来データ対応 初期版

### 作成ファイル

`08_python/convert_revit_schedule.py`

### 入力TXT

`03_input_csv/door_schedule_SD_export_test_v001.txt`

### 変換後CSV

`03_input_csv/door_schedule_converted_v001.csv`

### 品質チェック結果CSV

`04_output_csv/check_results_revit_v001.csv`

### 目的

Revitから書き出した集計表TXTをPython/pandasで読み込み、品質チェック用CSVへ変換したうえで、既存の品質チェックツールに入力できるかを検証します。

この初期版では、ドア集計表TXTを対象に、Revit由来データでも品質チェック結果CSVを出力できることを確認します。

### 処理フロー

```text
Revit書き出しTXT
↓
convert_revit_schedule.py で品質チェック用CSVへ変換
↓
door_schedule_converted_v001.csv
↓
check_bim_quality.py で品質チェック
↓
check_results_revit_v001.csv を出力
```

### 実行結果

`convert_revit_schedule.py` により、Revit書き出しTXTを以下のように変換しました。

```text
元データ：26行 × 33列
変換後：26行 × 10列
```

その後、変換後CSVを `check_bim_quality.py` に読み込ませ、以下の品質チェック結果を出力しました。

```text
104 issues found
```

### 確認結果

`check_results_revit_v001.csv` をExcelで開き、以下の列が出力されていることを確認しました。

- RuleId
- RuleName
- Severity
- FixGuide
- SourceFile
- DetectedAt
- ModelName

### 補足

今回のRevit由来CSV対応は初期試作です。

`door_schedule_converted_v001.csv` では、現時点で `BIM_ClassificationCode`、`BIM_ModelRole`、`BIM_Zone` が空欄のため、R-001、R-002の未入力違反が多く検出されています。

また、元TXTの列対応は仮設定です。特に、元TXTのどの列が `ElementId`、`FamilyName`、`TypeName` に相当するかは、今後確認が必要です。

今回の目的は、正確な品質評価ではなく、Revit書き出しTXTから品質チェック結果CSVを出力する一連の処理フローが動くことを確認することです。

## Power BIダッシュボード 初期版

### 作成ファイル

`05_powerbi/bim_quality_dashboard_v001.pbix`

### 入力データ

`04_output_csv/check_results_v001.csv`

### 目的

Python品質チェックツールで出力したチェック結果CSVをPower BIに取り込み、BIM品質チェック結果を可視化します。

### 作成した可視化

- 総違反件数カード
- RuleId別違反件数
- Severity別違反件数
- Category別違反件数
- 違反一覧テーブル

### 確認できる内容

- 全体の違反件数
- どのRuleIdの違反が多いか
- 重大度の内訳
- カテゴリ別の違反件数
- 各違反の詳細内容と修正方針

### 確認結果

`check_results_v001.csv` をPower BI Desktopに読み込み、1ページの初期ダッシュボードを作成しました。

RuleId別、Severity別、Category別に違反件数を確認できます。

詳細確認用として、CheckId、ElementId、Category、FamilyName、RuleId、RuleName、Severity、Status、FixGuide を含む違反一覧テーブルを作成しました。

### 補足

Power BIは主役ではなく、Pythonで処理したBIM品質データの分析結果を可視化するために使用します。

初期版では1ページ構成とし、複雑なDAXや複数ページ構成は行いません。

## RuleId検索デモ 初期版

### 作成ファイル

`06_ai_demo/ruleid_lookup_demo.py`

### 参照データ

`02_rule_master/bim_rule_master_v001.csv`

### 目的

Power BIで確認したRuleIdをもとに、該当するBIM品質ルールの内容、重大度、業務影響、AI活用時の影響、修正方針を確認できるようにします。

### 実行コマンド

```powershell
python .\06_ai_demo\ruleid_lookup_demo.py
```

### 確認できる内容

- RuleId
- RuleName
- Category
- Severity
- TargetField
- CheckLogic
- BusinessImpact
- AIUseImpact
- FixGuide
- Reference

### 確認結果

R-001、R-002、R-003 を入力し、それぞれ該当ルールの内容を表示できることを確認しました。

存在しないRuleIdまたは空欄入力時には、該当するRuleIdが見つからない旨を表示します。  
`q` を入力すると終了できます。

### 補足

初期版では生成AIは使用せず、RuleIdをキーにルールマスタを検索する構成とします。

将来的には、検索したルール内容を生成AIに渡し、改善方法や業務影響を説明するミニデモへ拡張します。

## RuleId連携型 プロンプト生成デモ 初期版

### 作成ファイル

`06_ai_demo/ruleid_prompt_generator_demo.py`

### 参照データ

`02_rule_master/bim_rule_master_v001.csv`

### 出力先

`06_ai_demo/generated_prompts`

### 目的

RuleIdをキーにルールマスタから該当ルールを取得し、生成AIに渡すためのプロンプトを自動生成します。

現時点では、OpenAI APIなどを直接呼び出すのではなく、生成AIに渡すためのプロンプト文を作成するところまでを検証しています。

### 生成済みプロンプト

- `06_ai_demo/generated_prompts/prompt_R-001_20260517_172551.txt`
- `06_ai_demo/generated_prompts/prompt_R-002_20260517_172615.txt`
- `06_ai_demo/generated_prompts/prompt_R-003_20260517_172644.txt`

### 確認結果

R-001、R-002、R-003 を入力し、それぞれ該当ルールの内容をもとに生成AI用プロンプトを作成できることを確認しました。

作成したプロンプトはtxtファイルとして保存済みです。

### プロンプトに含めた条件

- 参照ルールに書かれている内容を優先する
- 参照元RuleIdを必ず明記する
- 資料にない内容は断定しない
- 自動修正を前提にしない
- 設計判断、施工判断、モデル修正の最終判断は人間が行う

### 補足

初期版ではOpenAI APIなどを使って生成AIを直接呼び出さず、AIに渡すプロンプトを生成する構成とします。

これにより、APIキー管理や従量課金を避けながら、RuleIdを起点に参照元を限定した生成AI活用設計を示します。

この処理は、本格的なRAGやローカルLLM連携の前段階として位置づけます。

## 5月成果物一覧

5月時点で作成・確認した成果物は以下のとおりです。

1. PoC管理フォルダ  
   `bim_quality_poc`

2. README  
   `README.md`

3. 検証用Revitモデル  
   `01_revit_model/BIM_Quality_Check_Sample_Model_R2024_v001.rvt`

4. Revit集計表書き出しテストファイル  
   `03_input_csv/door_schedule_SD_export_test_v001.txt`  
   `03_input_csv/material_schedule_export_test_v001.txt`  
   `03_input_csv/room_finish_schedule_export_test_v001.txt`

5. RuleIdルールマスタ初版  
   `02_rule_master/bim_rule_master_v001.csv`

6. 入力CSV初版  
   `03_input_csv/sample_bim_data_v001.csv`

7. Python品質チェックツール初期版  
   `08_python/check_bim_quality.py`

8. チェック結果CSV  
   `04_output_csv/check_results_v001.csv`

9. Power BIダッシュボード初期版  
   `05_powerbi/bim_quality_dashboard_v001.pbix`

10. ポートフォリオPDF骨子メモ  
    `07_portfolio/portfolio_outline_v001.md`

11. RuleId検索デモ初期版  
    `06_ai_demo/ruleid_lookup_demo.py`

12. RuleId連携型 プロンプト生成デモ初期版  
    `06_ai_demo/ruleid_prompt_generator_demo.py`  
    `06_ai_demo/generated_prompts/prompt_R-001_20260517_172551.txt`  
    `06_ai_demo/generated_prompts/prompt_R-002_20260517_172615.txt`  
    `06_ai_demo/generated_prompts/prompt_R-003_20260517_172644.txt`

13. Pythonコード説明メモ  
    `00_docs/python_code_explanation_memo.md`

14. Revit書き出しTXT読み込みテスト  
    `08_python/test_read_revit_txt.py`

15. Revit集計表TXT変換スクリプト試作  
    `08_python/convert_revit_schedule.py`

16. Revit由来変換CSV  
    `03_input_csv/door_schedule_converted_v001.csv`

17. Revit由来データ品質チェック結果CSV  
    `04_output_csv/check_results_revit_v001.csv`

18. 画面・資料用スクリーンショット  
    `07_portfolio/screenshots/powerbi_dashboard_v001.png`  
    `07_portfolio/screenshots/check_results_csv_v001.png`  
    `07_portfolio/screenshots/ruleid_lookup_demo_v001.png`  
    `07_portfolio/screenshots/prompt_generator_demo_v001.png`  
    `07_portfolio/screenshots/generated_prompt_txt_v001.png`

19. 特徴量作成構想メモ  
    `00_docs/create_bim_features_concept_memo.md`

20. 修正優先度分類モデル設計メモ  
    `00_docs/fix_priority_model_design_memo.md`

## 5月時点で説明できること

Autodesk日本仕様のRevitサンプルモデルを検証用データとして準備し、既存集計表からドア・部屋・材料のデータをTXT形式で書き出せることを確認しました。

そのうえで、BIM品質チェック用のRuleIdルールマスタを作成し、検証用サンプルCSVに対してPythonで必須パラメータ未入力、分類コード未入力、ファミリ命名規則違反を検出する初期版ツールを作成しました。

チェック結果はCSVとして出力し、Power BIで総違反件数、RuleId別、重大度別、カテゴリ別に可視化する1ページダッシュボードを作成しました。

さらに、Power BIで確認したRuleIdを起点に、該当ルールの内容、重大度、業務影響、AI活用時の影響、修正方針を確認できるRuleId検索デモを作成しました。

加えて、RuleIdをキーに該当ルールだけを取得し、生成AIに渡すためのプロンプトを自動生成するデモを作成しました。初期版ではOpenAI APIなどを直接呼び出さず、APIキー管理や従量課金を避けながら、参照元を限定した生成AI活用設計を示す構成としています。

さらに、Revit書き出しTXT `door_schedule_SD_export_test_v001.txt` をPython/pandasで読み込み、`convert_revit_schedule.py` により品質チェック用CSV `door_schedule_converted_v001.csv` へ変換しました。その変換後CSVを `check_bim_quality.py` に入力し、Revit由来データの品質チェック結果として `check_results_revit_v001.csv` を出力できることを確認しました。

また、主要Pythonコード3本について、目的、処理フロー、主要関数の役割を日本語で整理し、PoCの処理内容を説明できる状態にしました。

## 進捗評価

当初の5月予定だった準備・設計に加え、6月予定のRevit由来データ対応の初期試作まで前倒しで完了しました。

現時点では、Revit書き出しTXTを `convert_revit_schedule.py` で品質チェック用CSVへ変換し、そのCSVを `check_bim_quality.py` に読み込ませて、RuleId付きの品質チェック結果 `check_results_revit_v001.csv` を出力できることを確認しています。

また、7月以降に予定している特徴量作成、品質スコア、修正優先度分類モデルについても、構想メモ・設計メモを作成し、今後の拡張方針を整理しました。

ただし、Revit由来データ対応は初期試作であり、列マッピングは仮設定です。現時点の結果は正確な品質評価ではなく、Revit由来データでも一連の処理フローが動くことを確認するための検証として位置づけます。

## 今後の拡張予定

今後は、作成済みの初期PoCを、AIエンジニア向けポートフォリオとして説明しやすい形に整理します。

### 作成済み

- RuleId検索デモ
- RuleId連携型 プロンプト生成デモ
- READMEの整理
- ポートフォリオPDF骨子メモ
- Pythonコード説明メモ
- Revit書き出しTXT読み込みテスト
- Revit集計表TXT変換スクリプト試作
- Revit由来データ品質チェック結果CSV
- 画面・資料用スクリーンショット
- 特徴量作成構想メモ
- 修正優先度分類モデル設計メモ

### 追加予定

- Revit集計表から書き出したTXTデータの自動整形
- Revit由来CSVの整形版
- BIM品質チェック結果の特徴量化
- 要素ごとの品質スコア算出
- 修正優先度 High / Medium / Low の分類
- scikit-learnを用いた簡易機械学習モデルの検証
- Streamlitによる簡易UI化
- 生成AI向けプロンプト生成の高度化
- 将来的なRAG構成、ローカルLLM、Azure AI連携への拡張
- ポートフォリオPDF本体
- 職務経歴書への反映
- AI活用範囲と人間確認範囲の整理
- 開発部門へ渡せる仕様整理
- 面接用説明文の整理

## 次工程の優先順位

1. Revit集計表の各列の意味を確認し、正式な列マッピングを整理する
2. `convert_revit_schedule.py` をv0.2化し、列名付与・必要列抽出・空欄処理を整理する
3. `check_bim_quality.py` をv0.2化し、入力CSV・出力CSVを指定しやすくする
4. Revit由来データ対応の実行手順をREADMEに整理する
5. BIM品質チェック結果から特徴量を作成する
6. 品質スコアを算出する
7. 修正優先度 High / Medium / Low の仮ラベルを作成する
8. Power BIダッシュボードをRevit由来データ対応版に更新する
9. ポートフォリオPDF本体に整理する
10. 職務経歴書へBIM×AI/Data Engineeringのストーリーとして反映する

## 補足

初期PoCでは、Python品質チェック、Power BI可視化、RuleId検索、RuleId連携型プロンプト生成まで完了しています。

次工程では、手作りの検証用CSVだけでなく、Revit由来TXTをPythonで整形し、CSVへ変換したうえで同じ品質チェックの流れを検証します。

また、AIをどこまで支援に使い、どこから人間が確認・判断するかを整理することで、建設業務における安全な生成AI活用設計として説明できるようにします。

## ポートフォリオでの説明文

このPoCでは、Revit/BIMデータを対象に、Python/pandasによるデータ読み込み、RuleIdベースの品質チェック、チェック結果CSV出力、Power BIによる可視化、RuleId検索、生成AI向けプロンプト生成までを実装しました。

初期版では、品質チェック用に整形したサンプルCSVを入力として使用し、必須パラメータ未入力、分類コード未入力、ファミリ命名規則違反をRuleId付きで検出しました。

その後、Revitから書き出した集計表TXTを `convert_revit_schedule.py` で品質チェック用CSVへ変換し、そのCSVを `check_bim_quality.py` に入力して、Revit由来データの品質チェック結果 `check_results_revit_v001.csv` を出力できることを確認しました。

現時点のRevit由来データ対応は初期試作であり、列マッピングは仮設定です。今後は、Revit集計表の列構成を整理し、特徴量作成、品質スコア算出、簡易機械学習モデルへ拡張する予定です。

BIMデータをAI・機械学習・データ分析で扱うための前処理、ルールベース判定、分析用データ出力、生成AI活用前段階の設計を一連のPoCとして構築しています。