# BIM Data Quality Engineering & AI Analysis PoC

## 管理情報

PoC管理フォルダ：  
`bim_quality_poc`

PoC名：  
`BIM Data Quality Engineering & AI Analysis PoC`

日本語名：  
`BIMデータ品質エンジニアリング・AI分析PoC`

---

## Overview

このPoCは、Revit/BIMデータをPythonで処理し、BIM品質ルールに基づく品質チェック、品質メトリクス作成、特徴量設計、機械学習プロトタイプ、生成AI向け構造化コンテキスト生成までを検証するものです。

目的は、建築BIMデータをAI・機械学習・データ分析で扱うために必要となる、データ整形、ルールベース判定、品質評価、特徴量設計、品質スコア算出、修正優先度分類プロトタイプ、生成AI連携前処理の一連の流れを実装することです。

現時点では、品質チェック用に整形されたBIMデータCSVを入力として使用し、Revitから書き出した集計表TXTをPythonで読み込み、品質チェック用CSVへ変換したうえで、品質チェック結果CSVを出力する初期試作まで確認済みです。

本PoCでは、BIMデータを単なる設計情報ではなく、AI・機械学習・データ分析で扱うための構造化データとして捉え、Pythonによるデータ処理、品質評価、特徴量設計、AI活用前提のデータ設計を行います。

なお、本PoCはAIモデルそのものの精度を追求するものではなく、建築BIMデータをAI・機械学習で扱うために必要なデータ整形、品質評価、特徴量設計、分析、機械学習プロトタイプ、生成AI連携前処理までの一連の流れを検証するものです。

---

## Purpose

このPoCの目的は、BIM/Revit導入支援の経験を活かしながら、建築BIMデータをAI・機械学習・データ分析で扱うためのデータ処理パイプラインを設計・実装することです。

単なるBIM品質チェックツールではなく、Revit/BIMデータをPythonで読み込み、データ整形、品質チェック、品質メトリクス作成、特徴量設計、品質スコア算出、機械学習プロトタイプ、生成AI向け構造化コンテキスト生成へつなげる一連の流れを検証します。

特に、以下の点を重視しています。

- PythonによるRevit/BIMデータ処理
- pandasによるデータ読み込み・整形・欠損処理
- RuleIdを用いたBIM品質ルール管理
- ルールベースによるBIMデータ品質チェック
- BIM品質チェック結果のCSV出力
- 品質メトリクス、品質スコア、特徴量データセットの設計
- scikit-learnを用いた機械学習プロトタイプへの拡張
- Streamlitによる簡易UI化
- Power BIによる補助的な可視化
- RuleId、違反内容、重大度、修正優先度をもとにした生成AI向け構造化コンテキスト生成
- 将来的なRAG、Azure AI、ローカルLLM、pyRevit、Revit API連携への拡張

---

## Background

BIM導入支援の現場では、Revitモデルそのものを作成するだけでなく、BIMデータをどのように標準化し、品質管理し、分析やAI活用に接続するかが重要になります。

BIMデータに未入力項目、分類コード不足、命名規則違反、属性情報のばらつきがある場合、そのままではBI、機械学習、生成AI、RAGなどに活用しにくくなります。

このPoCでは、BIM導入支援で扱ってきた品質ルールや運用ルールを、Pythonで処理可能なデータとして整理し、BIMデータをAI・機械学習で扱える状態に整えることを目指します。

---

## このPoCで示したいこと

- Revit/BIMデータをPythonで読み込み、構造化データとして処理できる
- Revit集計表TXTをpandasで読み込み、品質チェック用CSVへ変換できる
- BIM品質ルールをRuleIdとして定義し、ルールベース判定を実装できる
- BIM導入支援で扱ってきた品質ルールや運用ルールを、Pythonで処理可能なデータとして整理できる
- pandasを用いてデータの読み込み、欠損判定、分類、集計ができる
- BIM品質チェック結果をRuleId、重大度、修正ガイド付きのCSVとして出力できる
- BIM品質チェック結果から、品質メトリクスや分析用データを作成できる
- 要素ごとの違反数、未入力項目数、重大度スコア、品質スコアなどの特徴量を設計できる
- BIM品質チェック結果を、機械学習や分析に利用できる特徴量データセットへ変換できる
- 修正優先度 High / Medium / Low の仮ラベルを設計し、修正優先度分類プロトタイプへ拡張できる
- scikit-learnを用いた修正優先度分類プロトタイプの試作、評価、限界整理へ発展できる
- Streamlitで、品質指標、違反傾向、修正優先度、生成AI向け構造化コンテキストを可視化できる
- Power BIを補助的な可視化手段として使える
- RuleId、違反内容、重大度、修正優先度、修正方針をもとに、生成AI向け構造化コンテキストを生成できる
- 生成AIに自由回答させるのではなく、参照ルールや入力情報を制御したAI活用設計ができる
- 将来的なRAG、Azure AI、ローカルLLM、pyRevit、Revit API連携に拡張できる設計にする

---

## Current Status

現時点では、品質チェック用に整形されたBIMデータCSVを入力として使用しています。

当初は、Python品質チェックロジックの検証用として、手作成のサンプルCSV `sample_bim_data_v001.csv` を入力に使用しました。

その後、Revitから書き出した集計表TXT `door_schedule_SD_export_test_v001.txt` を `convert_revit_schedule.py` で品質チェック用CSV `door_schedule_converted_v001.csv` へ変換し、そのCSVを `check_bim_quality.py` に入力して、品質チェック結果 `check_results_revit_v001.csv` を出力できることを確認しました。

現時点の処理フローは以下です。

```text
Revit書き出しTXT
↓
convert_revit_schedule.py で品質チェック用CSVへ変換
↓
door_schedule_converted_v001.csv
↓
check_bim_quality.py で品質チェック
↓
check_results_revit_v001.csv を出力
```

この構成により、Revit/BIM由来のデータをPythonで前処理し、品質チェック、分析、AI活用につなげる流れを段階的に構築しています。

なお、現時点のRevit由来データ対応は初期試作であり、列マッピングは仮設定です。今後、Revit集計表の各列の意味を確認し、正式な列マッピングを整理します。

---

## Data Source

Autodesk日本仕様 意匠サンプルモデル Revit 2024を検証用データとして使用します。

実案件データ・社外秘データは使用しません。

このPoCは、転職活動用ポートフォリオとして説明しやすいように、公開可能なサンプルデータを前提に構成しています。

---

## Target Categories

初期PoCでは、対象カテゴリを以下の3つに限定します。

- 壁 / Walls
- ドア / Doors
- 部屋 / Rooms

---

## Existing Schedule Export Check

初期PoCの設計上の対象カテゴリは「壁・ドア・部屋」とします。

ただし、5月時点で確認した既存集計表では、壁要素を直接一覧化した集計表が見当たらなかったため、CSV書き出しテストでは代替確認として「04 材料表 作業用」を使用しています。

これは5月時点の書き出し確認用の扱いであり、必要に応じて後続工程で壁集計表を新規作成します。

確認済み集計表は以下です。

- 20 ドア 建具表 SD  
  出力ファイル：`03_input_csv/door_schedule_SD_export_test_v001.txt`

- 04 材料表 作業用  
  出力ファイル：`03_input_csv/material_schedule_export_test_v001.txt`

- 05 部屋 仕上表  
  出力ファイル：`03_input_csv/room_finish_schedule_export_test_v001.txt`

上記3ファイルは、Revitからタブ区切りテキストとして書き出し、Excelで開けることを確認済みです。

---

## Out of Scope

初期PoCでは、以下は対象外とします。

- 床
- 天井
- 窓
- 家具
- 設備
- 構造
- 外構

---

## Tech Stack

現時点で使用している技術は以下です。

- Python 3.12.10
- pandas 3.0.3
- CSV / TXT
- Revit Schedule Export
- Power BI Desktop
- Markdown
- RuleId-based Quality Check

今後追加予定の技術は以下です。

- scikit-learn
- Streamlit
- JSON / Markdown Context Generation
- RAG
- Azure AI
- pyRevit
- Revit API

---

## Setup

PowerShellで、`bim_quality_poc` フォルダへ移動します。

```powershell
cd path\to\bim_quality_poc
```

`requirements.txt` を使用して、必要なライブラリをインストールします。

```powershell
pip install -r requirements.txt
```

pandas のバージョンを確認します。

```powershell
python -c "import pandas as pd; print(pd.__version__)"
```

確認結果：

```text
3.0.3
```

---

## requirements.txt

配置場所：

```text
bim_quality_poc/requirements.txt
```

現時点の記載内容：

```text
pandas==3.0.3
```

目的は、Python品質チェックツールの実行に必要な外部ライブラリを明示し、別環境でも再現しやすくすることです。

現時点では、CSV読み込み、データチェック、チェック結果CSV出力に pandas を使用します。

Excelファイルの直接読み書きやExcelレポート出力を追加する場合は、将来的に openpyxl の追加を検討します。

今後、scikit-learn、streamlit などを使う段階で追記します。

---

## Folder Structure

```text
bim_quality_poc/
├── README.md
├── requirements.txt
├── 01_revit_model/
│   └── BIM_Quality_Check_Sample_Model_R2024_v001.rvt
├── 02_rule_master/
│   └── bim_rule_master_v001.csv
├── 03_input_csv/
│   ├── sample_bim_data_v001.csv
│   ├── door_schedule_SD_export_test_v001.txt
│   ├── material_schedule_export_test_v001.txt
│   ├── room_finish_schedule_export_test_v001.txt
│   └── door_schedule_converted_v001.csv
├── 04_output_csv/
│   ├── check_results_v001.csv
│   └── check_results_revit_v001.csv
├── 05_powerbi/
│   └── bim_quality_dashboard_v001.pbix
├── 06_ai_demo/
│   ├── ruleid_lookup_demo.py
│   ├── ruleid_prompt_generator_demo.py
│   └── generated_prompts/
├── 07_portfolio/
│   ├── portfolio_outline_v001.md
│   └── screenshots/
├── 08_python/
│   ├── check_bim_quality.py
│   ├── test_read_revit_txt.py
│   └── convert_revit_schedule.py
├── 00_docs/
│   ├── python_code_explanation_memo.md
│   ├── create_bim_features_concept_memo.md
│   ├── fix_priority_model_design_memo.md
│   ├── clean_bim_data_design_memo.md
│   ├── calculate_quality_metrics_design_memo.md
│   └── generate_ai_context_design_memo.md
├── docs/
│   ├── system_overview.md
│   └── portfolio_summary.md
└── src/
    ├── convert_revit_schedule.py
    ├── clean_bim_data.py
    ├── check_bim_quality.py
    ├── calculate_quality_metrics.py
    ├── create_bim_features.py
    ├── train_fix_priority_model.py
    ├── generate_ai_context.py
    └── utils.py
```

---

## Folder Role

現時点の各フォルダの役割は以下です。

```text
08_python/：5月時点のPython試作コード
06_ai_demo/：5月時点のRuleId検索・生成AI向け説明生成デモ
src/：6月以降に整理する本実装用コード
docs/：設計資料・ポートフォリオ説明資料
00_docs/：5月時点の設計メモ・コード説明メモ
05_powerbi/：補助的な可視化成果物
07_portfolio/：ポートフォリオPDF・説明資料・スクリーンショット素材
```

6月以降は、`08_python` と `06_ai_demo` の試作コードを整理し、`src` 配下へ段階的に移行します。

---

## RuleId-based Quality Check

### RuleIdルールマスタ

ファイル名：

```text
02_rule_master/bim_rule_master_v001.csv
```

目的：

Python品質チェック、Power BI可視化、RuleId検索デモ、生成AI向け構造化コンテキスト生成で共通して使用するチェックルールIDを定義します。

初期ルール：

- R-001：必須パラメータ未入力
- R-002：分類コード未入力
- R-003：ファミリ命名規則違反

初期版では3ルールのみを定義します。

ルール数を増やすことよりも、RuleIdを共通キーとして、Pythonチェック結果、補助的な可視化、RuleId検索、生成AI向け構造化コンテキスト生成を接続することを重視します。

---

## Input CSV for Initial Test

ファイル名：

```text
03_input_csv/sample_bim_data_v001.csv
```

目的：

Python品質チェックの初期実装に使用する検証用サンプルCSVです。

含まれるカテゴリ：

- Doors
- Rooms
- Walls

主な列：

- ElementId
- Category
- FamilyName
- TypeName
- Level
- BIM_ClassificationCode
- BIM_ModelRole
- BIM_Zone
- SourceFile
- ModelName

R-001、R-002、R-003 のチェックを検証できるよう、意図的に未入力や命名規則違反を含めています。

意図的に含めた検証用エラー：

- BIM_ClassificationCode 未入力
- BIM_ModelRole 未入力
- BIM_Zone 未入力
- FamilyName の命名規則違反

このファイルはPython品質チェックロジックを作成するための検証用CSVであり、Revitから直接書き出したデータではありません。

---

## Run: Initial Quality Check

作成ファイル：

```text
08_python/check_bim_quality.py
```

入力CSV：

```text
03_input_csv/sample_bim_data_v001.csv
```

ルールマスタ：

```text
02_rule_master/bim_rule_master_v001.csv
```

出力CSV：

```text
04_output_csv/check_results_v001.csv
```

実行コマンド：

```powershell
python .\08_python\check_bim_quality.py
```

実行結果：

```text
9 issues found
```

検出対象ルール：

- R-001：必須パラメータ未入力
- R-002：分類コード未入力
- R-003：ファミリ命名規則違反

確認結果：

PowerShellで実行でき、`check_results_v001.csv` が出力されました。

出力CSVには、RuleId、RuleName、Severity、Status、FixGuide、DetectedAt が含まれています。  
Excelで開けることも確認済みです。

初期版ではRevit APIや自動修正は行わず、CSVを読み込み、BIM品質ルールに基づいて品質チェック結果を出力することを目的とします。

この処理は、BIMデータをAI・機械学習・データ分析に利用するための前処理として位置づけます。

---

## Revit Schedule TXT Conversion

作成ファイル：

```text
08_python/convert_revit_schedule.py
```

入力TXT：

```text
03_input_csv/door_schedule_SD_export_test_v001.txt
```

変換後CSV：

```text
03_input_csv/door_schedule_converted_v001.csv
```

品質チェック結果CSV：

```text
04_output_csv/check_results_revit_v001.csv
```

目的：

Revitから書き出した集計表TXTをPython/pandasで読み込み、品質チェック用CSVへ変換したうえで、既存の品質チェックツールに入力できるかを検証します。

この初期版では、ドア集計表TXTを対象に、Revit由来データでも品質チェック結果CSVを出力できることを確認します。

処理フロー：

```text
Revit書き出しTXT
↓
convert_revit_schedule.py で品質チェック用CSVへ変換
↓
door_schedule_converted_v001.csv
↓
check_bim_quality.py で品質チェック
↓
check_results_revit_v001.csv を出力
```

実行結果：

```text
元データ：26行 × 33列
変換後：26行 × 10列
```

その後、変換後CSVを `check_bim_quality.py` に読み込ませ、以下の品質チェック結果を出力しました。

```text
104 issues found
```

確認結果：

`check_results_revit_v001.csv` をExcelで開き、以下の列が出力されていることを確認しました。

- RuleId
- RuleName
- Severity
- FixGuide
- SourceFile
- DetectedAt
- ModelName

---

## Visualization

### Streamlit

Streamlitは、今後の主UI候補として位置づけます。

今後、以下を画面上で確認できる簡易UIを作成する予定です。

- CSVアップロード
- データプレビュー
- 品質チェック実行
- 違反一覧表示
- 品質スコア表示
- 修正優先度分類プロトタイプの結果表示
- 生成AI向け構造化コンテキスト表示
- CSV / JSON ダウンロード

### Power BI

Power BIは、Pythonで処理したBIM品質データの分析結果を確認するための補助的な可視化手段として使用します。

作成ファイル：

```text
05_powerbi/bim_quality_dashboard_v001.pbix
```

入力データ：

```text
04_output_csv/check_results_v001.csv
```

作成した可視化：

- 総違反件数カード
- RuleId別違反件数
- Severity別違反件数
- Category別違反件数
- 違反一覧テーブル

確認できる内容：

- 全体の違反件数
- どのRuleIdの違反が多いか
- 重大度の内訳
- カテゴリ別の違反件数
- 各違反の詳細内容と修正方針

初期版では1ページ構成とし、複雑なDAXや複数ページ構成は行いません。

---

## RuleId Lookup Demo

作成ファイル：

```text
06_ai_demo/ruleid_lookup_demo.py
```

参照データ：

```text
02_rule_master/bim_rule_master_v001.csv
```

目的：

Power BIなどで確認したRuleIdをもとに、該当するBIM品質ルールの内容、重大度、業務影響、AI活用時の影響、修正方針を確認できるようにします。

実行コマンド：

```powershell
python .\06_ai_demo\ruleid_lookup_demo.py
```

確認できる内容：

- RuleId
- RuleName
- Category
- Severity
- TargetField
- CheckLogic
- BusinessImpact
- AIUseImpact
- FixGuide
- Reference

確認結果：

R-001、R-002、R-003 を入力し、それぞれ該当ルールの内容を表示できることを確認しました。

存在しないRuleIdまたは空欄入力時には、該当するRuleIdが見つからない旨を表示します。  
`q` を入力すると終了できます。

初期版では生成AIは使用せず、RuleIdをキーにルールマスタを検索する構成とします。

将来的には、検索したルール内容を生成AI向け構造化コンテキストへ接続します。

---

## RuleId-based AI Context Generation Demo

作成ファイル：

```text
06_ai_demo/ruleid_prompt_generator_demo.py
```

参照データ：

```text
02_rule_master/bim_rule_master_v001.csv
```

出力先：

```text
06_ai_demo/generated_prompts
```

目的：

RuleIdをキーにルールマスタから該当ルールを取得し、生成AIへ渡す情報を制御するための初期デモです。

現時点では、OpenAI APIなどを直接呼び出すのではなく、生成AIに渡すためのプロンプト文を作成するところまでを検証しています。

今後は、RuleId・違反内容・重大度・品質スコア・修正優先度を含む、生成AI向け構造化コンテキスト生成へ発展させます。

生成済みプロンプト：

- `06_ai_demo/generated_prompts/prompt_R-001_20260517_172551.txt`
- `06_ai_demo/generated_prompts/prompt_R-002_20260517_172615.txt`
- `06_ai_demo/generated_prompts/prompt_R-003_20260517_172644.txt`

確認結果：

R-001、R-002、R-003 を入力し、それぞれ該当ルールの内容をもとに生成AI用プロンプトを作成できることを確認しました。

作成したプロンプトはtxtファイルとして保存済みです。

プロンプトに含めた条件：

- 参照ルールに書かれている内容を優先する
- 参照元RuleIdを必ず明記する
- 資料にない内容は断定しない
- 自動修正を前提にしない
- 設計判断、施工判断、モデル修正の最終判断は人間が行う

これにより、APIキー管理や従量課金を避けながら、RuleIdを起点に参照元を限定した生成AI活用設計を示します。

この処理は、本格的なRAGやローカルLLM連携の前段階として位置づけます。

---

## Output Files

現時点で出力・確認済みの主なファイルは以下です。

```text
04_output_csv/check_results_v001.csv
04_output_csv/check_results_revit_v001.csv
03_input_csv/door_schedule_converted_v001.csv
06_ai_demo/generated_prompts/prompt_R-001_20260517_172551.txt
06_ai_demo/generated_prompts/prompt_R-002_20260517_172615.txt
06_ai_demo/generated_prompts/prompt_R-003_20260517_172644.txt
05_powerbi/bim_quality_dashboard_v001.pbix
```

---

## Current Deliverables

5月時点での主な成果は以下です。

- Revitサンプルモデルを使った検証環境の準備
- Revit集計表TXTの書き出し確認
- 検証用サンプルCSVの作成
- RuleIdルールマスタ初版の作成
- Python品質チェックツール初期版の作成
- RuleId付きチェック結果CSVの出力
- Power BIによる補助的な可視化
- RuleId検索デモの作成
- RuleIdベース生成AI向け構造化コンテキスト生成の前段階となるプロンプト生成デモの作成
- Revit書き出しTXTのPython/pandas読み込み確認
- Revit書き出しTXTから品質チェック用CSVへの変換試作
- Revit由来データを用いた品質チェック結果CSVの出力確認
- Pythonコード説明メモの作成
- 特徴量設計構想メモの作成
- 修正優先度分類プロトタイプ設計メモの作成
- clean_bim_data.py 設計メモの作成
- calculate_quality_metrics.py 設計メモの作成
- generate_ai_context.py 設計メモの作成
- system_overview.md の作成
- portfolio_summary.md の作成

詳細な5月成果物一覧は、別途 `docs/portfolio_summary.md` または `docs/may_deliverables_260524.md` に整理します。

---

## Limitations

現時点の制約と注意点は以下です。

- Revit由来データ対応は初期試作です。
- `door_schedule_converted_v001.csv` の列マッピングは仮設定です。
- `check_results_revit_v001.csv` の104件の違反は、現時点では正確な品質評価ではなく、処理フロー確認を目的とした結果です。
- `BIM_ClassificationCode`、`BIM_ModelRole`、`BIM_Zone` が空欄のため、未入力違反が多く検出されています。
- 機械学習プロトタイプは未実装です。
- 修正優先度 High / Medium / Low は、今後仮ラベルとして設計する予定です。
- 実務適用には、修正履歴、手戻り時間、担当者判断、プロジェクト条件、修正工数などの教師データが必要です。
- 現時点ではOpenAI APIなどは直接呼び出していません。
- 現時点ではRevit APIによる直接操作や自動修正は行っていません。
- 設計判断、施工判断、モデル修正の最終判断は人間が行う前提です。

---

## Future Work

今後は、作成済みの初期PoCを、AI・データ分析エンジニア向けポートフォリオとして説明しやすい形に整理します。

### Data Engineering

- Revit集計表から書き出したTXTデータの自動整形
- Revit由来CSVの列マッピング整理
- Revit由来CSVの整形版作成
- データ辞書作成
- RuleId仕様整理
- 品質チェック処理の `src` 配下への整理

### Quality Metrics / Feature Engineering

- BIM品質チェック結果の品質メトリクス作成
- 要素ごとの品質スコア算出
- BIM品質チェック結果の特徴量設計
- 修正優先度 High / Medium / Low の仮ラベル設計

### Machine Learning Prototype

- scikit-learnを用いた修正優先度分類プロトタイプ
- classification_report の出力
- confusion_matrix の出力
- feature_importance の出力
- evaluation_policy.md の作成
- limitations.md の更新

### Streamlit / Visualization

- Streamlitによる簡易UI化
- CSVアップロード
- 品質チェック結果表示
- 品質スコア表示
- 修正優先度表示
- 生成AI向け構造化コンテキスト表示
- CSV / JSON ダウンロード

### AI Context Generation

- RuleId、違反内容、重大度、品質スコア、修正優先度を含むJSON生成
- Markdown形式のAI向け説明文生成
- 生成AI向け構造化コンテキスト生成
- 将来的なRAG構成、ローカルLLM、Azure AI連携への拡張

### Portfolio

- GitHub整理
- ポートフォリオPDF本体作成
- 職務経歴書への反映
- AI活用範囲と人間確認範囲の整理
- 開発部門へ渡せる仕様整理
- 面接用説明文の整理

---

## Next Priorities

次工程の優先順位は以下です。

1. Revit集計表の各列の意味を確認し、正式な列マッピングを整理する
2. `convert_revit_schedule.py` をv0.2化し、列名付与・必要列抽出・空欄処理を整理する
3. `check_bim_quality.py` をv0.2化し、入力CSV・出力CSVを指定しやすくする
4. `08_python` と `06_ai_demo` の試作コードを、`src` 配下へ段階的に整理する
5. Revit由来データ対応の実行手順をREADMEに整理する
6. `data_dictionary.md` を作成する
7. `rule_specification.md` を作成する
8. `limitations.md` を作成する
9. BIM品質チェック結果から特徴量を設計する
10. 品質スコアを算出する
11. 修正優先度 High / Medium / Low の仮ラベルを作成する
12. Streamlitによる簡易UIを作成する
13. 生成AI向け構造化コンテキスト生成へ拡張する
14. ポートフォリオPDF本体に整理する
15. 職務経歴書へBIM×AI/Data Engineeringのストーリーとして反映する

---

## Portfolio Summary

このPoCでは、Revit/BIMデータを対象に、Python/pandasによるデータ読み込み、RuleIdベースの品質チェック、チェック結果CSV出力、補助的なPower BI可視化、RuleId検索、生成AI向け構造化コンテキスト生成の前段階となるプロンプト生成までを実装しました。

初期版では、品質チェック用に整形したサンプルCSVを入力として使用し、必須パラメータ未入力、分類コード未入力、ファミリ命名規則違反をRuleId付きで検出しました。

その後、Revitから書き出した集計表TXTを `convert_revit_schedule.py` で品質チェック用CSVへ変換し、そのCSVを `check_bim_quality.py` に入力して、Revit由来データの品質チェック結果 `check_results_revit_v001.csv` を出力できることを確認しました。

現時点のRevit由来データ対応は初期試作であり、列マッピングは仮設定です。

今後は、Revit集計表の列構成を整理し、品質メトリクス作成、特徴量設計、品質スコア算出、修正優先度分類プロトタイプ、生成AI向け構造化コンテキスト生成へ拡張する予定です。

BIMデータをAI・機械学習・データ分析で扱うための前処理、ルールベース判定、分析用データ出力、生成AI活用前段階の設計を一連のPoCとして構築しています。
````
